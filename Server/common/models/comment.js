module.exports = function(Comment) {
  /*Comment.report = function (comment_id, user_id, cb) {
    Comment.findById(comment_id, function (err, comment) {
      if (err) cb(err);
      var sum = 1;
      sum = sum + comment.numberOfReport;
      console.log(sum)
      Comment
    });
  }

  Comment.remoteMethod(
    'report',
    {
      http: {path: '/report', verb: 'patch'},
      accepts: [
        {arg: 'comment_id', type: 'string', http: { source: 'query' } },
        {arg: 'user_id', type: 'string', http: { source: 'query' } }
      ],
      returns: [
        {arg: 'comment', type: 'object'}
      ]
    }
  );*/
};
